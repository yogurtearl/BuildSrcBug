subprojects {
  apply plugin: "groovy"

  dependencies {
    compile localGroovy()
    compile gradleApi()
  }

// START SNIPPET addOToRootProject
rootProject.dependencies {
  runtime project(path)
}
// END SNIPPET addToRootProject
}
